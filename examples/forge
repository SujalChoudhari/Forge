forge:
  on: [Linux,Win]
  detect: "*.rs"
  vars:
    cc: gcc
    fs: [f1,f2,f3]
  run:
    - Linux
    - echo this is linux {cc}
    - Win
    - echo this is win {cc}

build:
  on: Linux
  run: cp {file}.cpp